---
export const prerender = false;
import Layout from '../layouts/Layout.astro';
import FeaturedArticle from '../components/ui/FeaturedArticle.astro';
import ArticleCard from '../components/ui/ArticleCard.astro';
import SkeletonLoader from '../components/ui/SkeletonLoader.astro';
import { categories } from '../data/categories';

// 🚀 New strategy: Render skeleton screen immediately, load data asynchronously
// Don't wait for database queries on server-side, provide instant page rendering
const isLoading = true;
---

<Layout>
  <div class="container">
    <!-- Loading status indicator -->
    <div id="loadingStatus" class="loading-status">
      <div class="loading-dot"></div>
      Loading articles...
    </div>
    
    <!-- Main featured article section -->
      <section class="hero-section">
        <h2>Featured Article</h2>
      
      <!-- Skeleton screen - initial display -->
      <div id="featuredSkeleton" class="skeleton-wrapper">
        <SkeletonLoader type="featured" count={1} />
      </div>
      
      <!-- Real content - displayed after async loading -->
      <div id="featuredContent" class="content-wrapper" style="display: none;">
        <!-- This will be dynamically populated by JavaScript -->
      </div>
    </section>
    
    <!-- Category selection section -->
    <section class="categories-section">
      <h2 class="section-title">Browse by Category</h2>
      
      <div class="categories-grid">
        {categories.map(category => (
          <a href={`/category/${category.id}`} class="category-card">
            <div class="category-icon" style={`background-color: ${category.color}`}>
              <span class="category-initial">{category.name.charAt(0)}</span>
            </div>
            <h3 class="category-name">{category.name}</h3>
          </a>
        ))}
      </div>
    </section>
    
    <!-- Latest news section -->
    <section class="latest-news">
      <h2 class="section-title">Latest News (Last 30 Days)</h2>
      
      <!-- Skeleton screen -->
      <div id="latestSkeleton" class="skeleton-wrapper">
        <SkeletonLoader type="card" count={6} />
      </div>
      
      <!-- Real content -->
      <div id="latestContent" class="content-wrapper" style="display: none;">
      <div class="latest-grid" id="latestArticlesGrid">
          <!-- Dynamic content -->
            </div>
      </div>
      
      <div class="view-all-container">
        <button id="toggleAllArticlesBtn" class="btn view-all-btn" type="button" aria-expanded="false" aria-controls="allArticlesSection">View All News</button>
      </div>
    </section>
    
    <!-- All articles section -->
    <section class="all-articles is-collapsed" id="allArticlesSection" aria-hidden="true">
      <h2 class="section-title">All Articles</h2>
      
      <!-- Skeleton screen -->
      <div id="allArticlesSkeleton" class="skeleton-wrapper">
        <SkeletonLoader type="list" count={10} />
      </div>
      
      <!-- Real content -->
      <div id="allArticlesContent" class="content-wrapper" style="display: none;">
      <div class="all-index" id="allArticlesList">
          <!-- Dynamic content -->
              </div>
        
        <!-- Pagination controls -->
        <div class="pagination-container" id="paginationContainer" style="display: none;">
          <div class="pagination-info">
            <span id="paginationInfo">Page 1 of 1</span>
          </div>
          <div class="pagination-controls">
            <button id="prevPageBtn" class="pagination-btn" disabled>
              <span>‹</span> Previous
            </button>
            <div class="page-numbers" id="pageNumbers">
              <!-- Page numbers will be generated dynamically -->
            </div>
            <button id="nextPageBtn" class="pagination-btn">
              Next <span>›</span>
            </button>
          </div>
        </div>
      </div>
    </section>
    
    <!-- Newsletter signup -->
    <section class="newsletter-section">
      <div class="newsletter-container">
        <div class="newsletter-content">
          <h2 class="newsletter-title">Stay Informed</h2>
          <p class="newsletter-description">
            Subscribe to our newsletter and never miss breaking news, exclusive stories, and important updates.
          </p>
        </div>
        
        <form class="newsletter-form">
          <input type="email" placeholder="Enter your email address" required>
          <button type="submit" class="btn">Subscribe</button>
        </form>
      </div>
    </section>
  </div>
  
  <!-- 🚀 优化后的首页加载策略 -->
  <script is:inline>
    // 使用轻量级加载器优先显示关键内容
    const script = document.createElement('script');
    script.src = '/scripts/lightweight-homepage-loader.js';
    script.async = true;
    script.onerror = () => {
      // 降级到原始加载器
      const fallbackScript = document.createElement('script');
      fallbackScript.src = '/scripts/progressive-loader.js';
      fallbackScript.async = true;
      document.head.appendChild(fallbackScript);
    };
    document.head.appendChild(script);
  </script>
</Layout>

<style>
  /* 🚀 Loading status styles */
  .loading-status {
    position: fixed;
    top: 80px;
    right: 20px;
    background: var(--color-primary);
    color: white;
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: 0.9rem;
    font-weight: 500;
    box-shadow: var(--shadow-lg);
    z-index: 1000;
    opacity: 1;
    transition: opacity 0.3s ease;
  }
  
  .loading-dot {
    width: 8px;
    height: 8px;
    background: currentColor;
    border-radius: 50%;
    animation: loading-bounce 1.4s infinite ease-in-out;
  }
  
  @keyframes loading-bounce {
    0%, 80%, 100% {
      transform: scale(0);
    }
    40% {
      transform: scale(1);
    }
  }
  
  /* Skeleton screen and content switching */
  .skeleton-wrapper,
  .content-wrapper {
    transition: opacity 0.3s ease;
  }
  
  .content-wrapper {
    opacity: 0;
  }
  
  .content-wrapper.loaded {
    opacity: 1;
  }
  
  .no-articles {
    text-align: center;
    padding: var(--space-8);
    color: var(--color-text-light);
  }
  
  .section-title {
    font-size: 1.75rem;
    margin-bottom: var(--space-6);
    position: relative;
    padding-bottom: var(--space-3);
    color: var(--color-text);
  }
  
  .section-title::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 50px;
    height: 3px;
    background-color: var(--color-primary);
  }
  
  .hero-section {
    margin-top: var(--space-6);
  }
  
  .categories-section {
    margin: var(--space-12) 0;
  }
  
  .categories-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-4);
  }
  
  .category-card {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-4);
    border-radius: var(--radius-md);
    background-color: var(--color-background);
    border: 1px solid var(--color-border);
    text-decoration: none;
    color: var(--color-text);
    transition: transform var(--transition), box-shadow var(--transition);
  }
  
  .category-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-md);
  }
  
  .category-icon {
    width: 50px;
    height: 50px;
    min-width: 50px;
    min-height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    color: white;
    font-weight: 700;
    font-size: 1.25rem;
    flex-shrink: 0;
  }
  
  .category-name {
    font-size: 1.125rem;
    margin: 0;
  }
  
  .latest-news,
  .all-articles {
    margin: var(--space-12) 0;
  }
  
  /* Latest small thumbnails grid (global so JS-inserted HTML is styled) */
  :global(.latest-grid) {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-6);
    margin-bottom: var(--space-8);
  }

  :global(.thumb-card) {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    text-decoration: none;
    color: var(--color-text);
    background-color: var(--color-background);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 0;
    transition: transform var(--transition), box-shadow var(--transition);
  }

  :global(.thumb-card:hover) {
    transform: translateY(-2px);
    box-shadow: var(--shadow-sm);
  }

  :global(.thumb-image-wrap) {
    position: relative;
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    border-radius: var(--radius-md);
  }

  :global(.thumb-img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: var(--radius-md);
  }

  :global(.thumb-gradient) {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, rgba(0,0,0,0.05) 20%, rgba(0,0,0,0.7) 100%);
    border-radius: var(--radius-md);
  }

  :global(.thumb-text) {
    position: absolute;
    left: var(--space-3);
    right: var(--space-3);
    bottom: var(--space-3);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  :global(.thumb-title) {
    color: #fff;
    font-weight: 700;
    font-size: 1.1rem;
    line-height: 1.35;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    text-shadow: 0 1px 2px rgba(0,0,0,0.5);
  }

  :global(.thumb-date) {
    font-size: 0.85rem;
    color: rgba(255,255,255,0.9);
    text-shadow: 0 1px 2px rgba(0,0,0,0.5);
  }

  /* All articles compact list (global for JS-inserted HTML) */
  :global(.all-index) {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  :global(.index-row) {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-2);
    text-decoration: none;
    color: var(--color-text);
    border-radius: var(--radius-sm);
    transition: background-color var(--transition);
  }

  :global(.index-row:hover) {
    background-color: var(--color-background-alt);
  }

  :global(.index-thumb) {
    width: 56px;
    height: 56px;
    border-radius: var(--radius-sm);
    object-fit: cover;
    flex-shrink: 0;
  }

  :global(.index-meta) {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  :global(.index-title) {
    font-size: 0.95rem;
    line-height: 1.3;
  }

  :global(.index-sub) {
    font-size: 0.8rem;
    color: var(--color-text-light);
  }
  
  .view-all-container {
    display: flex;
    justify-content: center;
    margin-top: var(--space-8);
  }
  
  .view-all-btn {
    padding: var(--space-4) var(--space-8);
    font-size: 1.05rem;
    font-weight: 600;
    min-width: 220px;
  }
  
  /* Collapsible All Articles */
  .all-articles.is-collapsed {
    max-height: 0;
    overflow: hidden;
    padding-top: 0;
    padding-bottom: 0;
    margin-top: 0;
    margin-bottom: 0;
    opacity: 0;
    transition: max-height 0.4s ease, opacity 0.3s ease, margin 0.3s ease, padding 0.3s ease;
  }
  .all-articles.is-expanded {
    max-height: 2000px; /* large enough */
    opacity: 1;
    transition: max-height 0.5s ease, opacity 0.3s ease;
  }
  
  .newsletter-section {
    margin: var(--space-12) 0;
  }
  
  .newsletter-container {
    background-color: var(--color-primary);
    color: white;
    padding: var(--space-8);
    border-radius: var(--radius-lg);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-6);
  }
  
  .newsletter-title {
    font-size: 2rem;
    margin-bottom: var(--space-3);
    color: #fff;
  }
  
  .newsletter-description {
    max-width: 600px;
    font-size: 1.125rem;
    margin: 0 auto;
    color: rgba(255,255,255,0.9);
  }
  
  .newsletter-form {
    display: flex;
    gap: var(--space-2);
    width: 100%;
    max-width: 500px;
    flex-direction: column;
  }
  
  .newsletter-form input {
    padding: var(--space-3) var(--space-4);
    border: none;
    border-radius: var(--radius-md);
    font-size: 1rem;
    flex: 1;
  }
  
  .newsletter-form .btn {
    background-color: rgba(255,255,255,0.15);
    color: #fff;
    padding: var(--space-3) var(--space-4);
    border: 1px solid rgba(255,255,255,0.5);
  }
  
  /* 🎯 Featured Article styles */
  :global(.featured-article) {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    height: 500px;
    margin-bottom: var(--space-8);
  }
  
  :global(.featured-image-container),
  :global(.featured-media-container) {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }
  
  :global(.featured-image) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  
  :global(.featured-overlay) {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.1) 0%,
      rgba(0, 0, 0, 0.8) 100%
    );
  }
  
  :global(.featured-content) {
    position: relative;
    z-index: 1;
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    height: 100%;
    color: white;
  }
  
  :global(.featured-title) {
    font-size: 1.75rem;
    margin: var(--space-3) 0;
    line-height: 1.2;
  }
  
  :global(.featured-title a) {
    color: white;
    text-decoration: none;
    transition: color var(--transition);
  }
  
  :global(.featured-title a:hover) {
    color: rgba(255, 255, 255, 0.8);
  }
  
  :global(.featured-excerpt) {
    margin-bottom: var(--space-4);
    font-size: 1rem;
    line-height: 1.5;
    max-width: 700px;
  }
  
  :global(.featured-meta) {
    display: flex;
    gap: var(--space-4);
    margin-bottom: var(--space-4);
    font-size: 0.875rem;
  }
  
  :global(.featured-author) {
    font-weight: 500;
  }
  
  :global(.featured-date) {
    opacity: 0.8;
  }
  
  :global(.featured-read-more) {
    align-self: flex-start;
  }
  
  :global(.featured-duration) {
    background: rgba(0, 0, 0, 0.7);
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 0.75rem;
    font-weight: 500;
  }
  
  :global(.category-tag) {
    align-self: flex-start;
    display: inline-block;
    padding: var(--space-1) var(--space-2);
    background-color: var(--color-primary);
    color: white;
    text-decoration: none;
    border-radius: var(--radius-sm);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: var(--space-10);
    transition: background-color var(--transition);
  }
  
  /* 🎯 已移动到global.css - 避免重复定义 */
  
  /* 🎯 Featured Article responsive styles */
  @media (min-width: 768px) {
    :global(.featured-article) {
      height: 600px;
    }
    
    :global(.featured-title) {
      font-size: 2.5rem;
      max-width: 800px;
    }
    
    :global(.featured-excerpt) {
      font-size: 1.125rem;
    }
  }
  
  @media (max-width: 640px) {
    :global(.featured-article) {
      height: 400px;
    }
    
    :global(.featured-title) {
      font-size: 1.5rem;
    }
    
    :global(.featured-excerpt) {
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
  }
  
  /* Responsive loading status */
  @media (max-width: 768px) {
    .loading-status {
      top: 70px;
      right: 10px;
      font-size: 0.8rem;
      padding: var(--space-2) var(--space-3);
    }
    
    .categories-grid {
      grid-template-columns: 1fr;
    }
    
    :global(.latest-grid) {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }
  }
  
  /* 🎥 Video Player Styles */
  :global(.featured-video) {
    position: relative;
  }
  
  /* 🎬 Video Card Styles for Lists */
  :global(.video-card .thumb-image-wrap) {
    position: relative;
  }
  
  :global(.video-badge) {
    position: absolute;
    top: 8px;
    left: 8px;
    background: rgba(139, 92, 246, 0.9);
    color: white;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 0.75rem;
    font-weight: 600;
    z-index: 2;
    display: flex;
    align-items: center;
    gap: 4px;
  }

  :global(.video-badge svg) {
    color: white;
    flex-shrink: 0;
  }
  
  /* 🎯 增强视频标识样式 - 纯白色版本 */
  :global(.video-badge.enhanced) {
    background: rgba(0, 0, 0, 0.7);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    border: 2px solid white;
    backdrop-filter: blur(10px);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: white !important;
    animation: video-badge-glow 2s infinite;
  }
  
  :global(.video-badge.enhanced svg) {
    color: white !important;
    stroke: white !important;
    fill: white !important;
  }
  
  :global(.video-badge.enhanced span) {
    color: white !important;
  }
  
  @keyframes video-badge-glow {
    0%, 100% { 
      border-color: white;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    }
    50% { 
      border-color: white;
      box-shadow: 0 4px 16px rgba(255, 255, 255, 0.3), 0 2px 8px rgba(0, 0, 0, 0.3);
    }
  }
  
  :global(.video-duration-badge) {
    position: absolute;
    bottom: 8px;
    right: 8px;
    background: rgba(0, 0, 0, 0.8);
    color: white;
    padding: 2px 6px;
    border-radius: 3px;
    font-size: 0.7rem;
    font-weight: 500;
    z-index: 2;
  }

  /* 🕒 主页Featured Article右上角YouTube时长显示 */
  :global(.featured-video-duration-badge-top-right) {
    position: absolute;
    top: 20px;
    right: 20px;
    background: rgba(0, 0, 0, 0.8);
    border: 2px solid white;
    color: white;
    padding: 8px 12px;
    border-radius: 6px;
    font-size: 0.875rem;
    font-weight: 700;
    z-index: 3;
    display: flex;
    align-items: center;
    gap: 4px;
    backdrop-filter: blur(10px);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    letter-spacing: 0.5px;
    text-transform: uppercase;
    animation: duration-badge-glow 3s infinite;
    transition: all 0.3s ease;
  }

  :global(.featured-video-duration-badge-top-right:hover) {
    background: rgba(0, 0, 0, 0.9);
    transform: scale(1.05);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4);
  }

  @keyframes duration-badge-glow {
    0%, 100% { 
      border-color: white;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    }
    50% { 
      border-color: rgba(255, 255, 255, 0.8);
      box-shadow: 0 4px 12px rgba(255, 255, 255, 0.2), 0 2px 8px rgba(0, 0, 0, 0.3);
    }
  }
  
  :global(.video-indicator) {
    color: #8b5cf6;
    margin-left: 6px;
    font-size: 0.85em;
    display: inline-flex;
    align-items: center;
  }

  :global(.video-indicator svg) {
    color: #8b5cf6;
  }
  
  /* 🎯 增强内联视频指示器 */
  :global(.video-indicator.enhanced) {
    color: #7c3aed;
    margin-left: 8px;
    padding: 2px 6px;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(168, 85, 247, 0.1));
    border-radius: 4px;
    border: 1px solid rgba(139, 92, 246, 0.3);
    transition: all 0.3s ease;
  }
  
  :global(.video-indicator.enhanced svg) {
    color: #7c3aed;
    filter: drop-shadow(0 1px 2px rgba(139, 92, 246, 0.3));
  }
  
  :global(.index-row:hover .video-indicator.enhanced) {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.2), rgba(168, 85, 247, 0.2));
    border-color: rgba(139, 92, 246, 0.5);
    transform: scale(1.05);
  }
  
  :global(.duration-text) {
    color: #6b7280;
    margin-left: 8px;
    font-size: 0.8rem;
  }
  
  :global(.video-row .index-thumb) {
    border: 2px solid #8b5cf6;
    border-radius: 6px;
  }
  
  :global(.featured-video-element) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    cursor: pointer;
  }
  
  :global(.video-overlay) {
    background: linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.1) 0%,
      rgba(0, 0, 0, 0.4) 50%,
      rgba(0, 0, 0, 0.8) 100%
    );
    pointer-events: none;
  }
  
  /* 🎮 Video Controls Container */
  :global(.featured-video-controls) {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0.8;
    transition: opacity 0.3s ease;
    pointer-events: none;
    z-index: 5;
  }
  
  :global(.featured-video:hover .featured-video-controls) {
    opacity: 1;
  }
  
  :global(.featured-video-controls > *) {
    pointer-events: all;
  }
  
  /* 🎯 Play/Pause Button */
  :global(.video-play-btn) {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: rgba(0, 0, 0, 0.7);
    border: 3px solid rgba(255, 255, 255, 0.9);
    border-radius: 50%;
    width: 80px;
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
    pointer-events: all;
    z-index: 10;
  }
  
  :global(.video-play-btn:hover) {
    background: rgba(0, 0, 0, 0.9);
    border-color: white;
    transform: translate(-50%, -50%) scale(1.1);
  }
  
  :global(.video-play-btn svg) {
    margin-left: 4px; /* Offset play icon for visual balance */
  }
  
  /* 🔲 Fullscreen Button */
  :global(.video-fullscreen-btn) {
    position: absolute;
    top: 20px;
    right: 20px;
    background: rgba(0, 0, 0, 0.7);
    border: 1px solid rgba(255, 255, 255, 0.5);
    border-radius: 6px;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
    pointer-events: all;
    z-index: 10;
  }
  
  :global(.video-fullscreen-btn:hover) {
    background: rgba(0, 0, 0, 0.9);
    border-color: white;
    transform: scale(1.05);
  }
  
  /* 📊 Progress Bar */
  :global(.video-progress-container) {
    position: absolute;
    bottom: 60px;
    left: 20px;
    right: 20px;
    height: 4px;
    cursor: pointer;
    pointer-events: all;
    z-index: 10;
  }
  
  :global(.video-progress-bar) {
    width: 100%;
    height: 100%;
    background: rgba(255, 255, 255, 0.3);
    border-radius: 2px;
    overflow: hidden;
    backdrop-filter: blur(5px);
  }
  
  :global(.video-progress-fill) {
    height: 100%;
    background: linear-gradient(90deg, #8b5cf6, #a855f7);
    width: 0%;
    transition: width 0.1s ease;
    border-radius: 2px;
  }
  
  :global(.video-progress-container:hover .video-progress-bar) {
    height: 6px;
    transform: translateY(-1px);
  }
  
  :global(.video-progress-container:hover .video-progress-fill) {
    background: linear-gradient(90deg, #7c3aed, #8b5cf6);
    box-shadow: 0 0 10px rgba(139, 92, 246, 0.5);
  }
  
  /* 🕒 Time Display */
  :global(.video-time-display) {
    position: absolute;
    bottom: 20px;
    right: 20px;
    background: rgba(0, 0, 0, 0.8);
    color: white;
    padding: 6px 10px;
    border-radius: 4px;
    font-size: 0.875rem;
    font-weight: 500;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }
  
  :global(.video-separator) {
    margin: 0 4px;
    opacity: 0.7;
  }
  
  /* 🎬 Video States */
  :global(.featured-video.playing .video-overlay) {
    opacity: 0.3;
  }
  
  :global(.featured-video.paused .featured-video-controls) {
    opacity: 1;
  }
  
  /* 📱 Mobile Video Controls */
  @media (max-width: 640px) {
    :global(.video-play-btn) {
      width: 60px;
      height: 60px;
    }
    
    :global(.video-fullscreen-btn) {
      width: 36px;
      height: 36px;
      top: 16px;
      right: 16px;
    }
    
    :global(.video-progress-container) {
      bottom: 50px;
      left: 16px;
      right: 16px;
    }
    
    :global(.video-time-display) {
      bottom: 16px;
      right: 16px;
      font-size: 0.75rem;
      padding: 4px 8px;
    }

    /* 📱 移动端主页Featured Article时长显示适配 */
    :global(.featured-video-duration-badge-top-right) {
      top: 16px;
      right: 16px;
      padding: 6px 8px;
      font-size: 0.75rem;
      border-width: 1px;
    }
  }
  
  /* 🌓 Dark Mode Video Controls */
  @media (prefers-color-scheme: dark) {
    :global(.video-play-btn) {
      background: rgba(0, 0, 0, 0.8);
      border-color: rgba(255, 255, 255, 0.8);
    }
    
    :global(.video-fullscreen-btn) {
      background: rgba(0, 0, 0, 0.8);
      border-color: rgba(255, 255, 255, 0.4);
    }
    
    :global(.video-time-display) {
      background: rgba(0, 0, 0, 0.9);
      border-color: rgba(255, 255, 255, 0.3);
    }
  }

  /* 🎨 现代化分页样式 - 基于网站设计系统 */
  .pagination-container {
    margin-top: var(--space-8);
    padding: var(--space-6) 0;
    border-top: 1px solid var(--color-border);
    background: linear-gradient(135deg, var(--color-background) 0%, var(--color-background-alt) 100%);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
  }

  .pagination-info {
    text-align: center;
    margin-bottom: var(--space-4);
    color: var(--color-primary);
    font-size: 0.875rem;
    font-weight: 600;
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    /* 降级处理 - 如果渐变不支持则显示主色 */
    color: var(--color-primary);
    text-shadow: 0 1px 2px rgba(26, 115, 232, 0.1);
  }

  .pagination-controls {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--space-3);
    flex-wrap: wrap;
  }

  .pagination-btn {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-4);
    background: var(--color-background);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    color: var(--color-text);
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: all var(--transition);
    min-width: 100px;
    justify-content: center;
    box-shadow: var(--shadow-sm);
    position: relative;
    overflow: hidden;
  }

  .pagination-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.5s ease;
  }

  .pagination-btn:hover:not(:disabled) {
    background: var(--color-primary);
    color: white;
    border-color: var(--color-primary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .pagination-btn:hover:not(:disabled)::before {
    left: 100%;
  }

  .pagination-btn:disabled {
    opacity: 0.4;
    cursor: not-allowed;
    transform: none;
    background: var(--color-background-alt);
    color: var(--color-text-light);
  }

  .page-numbers {
    display: flex;
    gap: var(--space-2);
    align-items: center;
    padding: var(--space-1);
    background: rgba(255, 255, 255, 0.5);
    border-radius: var(--radius-lg);
    box-shadow: inset 0 1px 3px rgba(0, 0, 0, 0.05);
  }

  .page-number {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-background);
    color: var(--color-text);
    font-size: 0.875rem;
    font-weight: 600;
    cursor: pointer;
    transition: all var(--transition);
    position: relative;
    overflow: hidden;
  }

  .page-number::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    background: radial-gradient(circle, var(--color-primary-light) 0%, var(--color-primary) 70%);
    border-radius: 50%;
    transform: translate(-50%, -50%);
    transition: all 0.3s ease;
    z-index: 0;
  }

  .page-number span {
    position: relative;
    z-index: 1;
    /* 🎨 与分页信息相同的渐变文字效果 */
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: var(--color-primary); /* 降级处理 */
    font-weight: 700;
  }

  .page-number:hover:not(.active) {
    background: var(--color-background-alt);
    border-color: var(--color-primary-light);
    transform: translateY(-1px);
    box-shadow: var(--shadow-sm);
  }

  .page-number:hover:not(.active)::before {
    width: 100%;
    height: 100%;
    opacity: 0.1;
  }

  .page-number.active {
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
    border-color: var(--color-primary);
    color: white;
    box-shadow: 0 4px 12px rgba(26, 115, 232, 0.3);
    transform: translateY(-1px);
  }

  .page-number.active::before {
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(255,255,255,0.2) 0%, transparent 70%);
  }

  .page-number.active span {
    /* 激活状态下使用白色文字以确保在蓝色背景上可读 */
    background: none;
    -webkit-background-clip: unset;
    -webkit-text-fill-color: unset;
    background-clip: unset;
    color: white;
    font-weight: 800;
  }

  .page-ellipsis {
    color: var(--color-text-light);
    padding: 0 var(--space-2);
    font-weight: 600;
    position: relative;
  }

  .page-ellipsis::before {
    content: '···';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    opacity: 0.5;
  }

  .no-articles {
    text-align: center;
    padding: var(--space-8);
    color: var(--color-text-light);
    font-style: italic;
    background: var(--color-background-alt);
    border-radius: var(--radius-md);
    border: 1px dashed var(--color-border);
  }

  /* 🌟 响应式设计 */
  @media (max-width: 640px) {
    .pagination-container {
      margin-top: var(--space-6);
      padding: var(--space-4);
    }
    
    .pagination-controls {
      flex-direction: column;
      gap: var(--space-4);
    }
    
    .page-numbers {
      order: -1;
      justify-content: center;
    }
    
    .pagination-btn {
      min-width: 120px;
      padding: var(--space-3) var(--space-6);
    }

    .page-number {
      width: 36px;
      height: 36px;
      font-size: 0.8rem;
    }
  }

  /* 🎭 加载状态动画 */
  .pagination-container.loading {
    opacity: 0.6;
    pointer-events: none;
  }

  .pagination-container.loading .pagination-btn,
  .pagination-container.loading .page-number {
    animation: pulse 1.5s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  /* 🎯 焦点和可访问性 */
  .pagination-btn:focus,
  .page-number:focus {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
    box-shadow: 0 0 0 3px rgba(26, 115, 232, 0.1);
  }

  /* 🌗 深色模式适配 */
  @media (prefers-color-scheme: dark) {
    .pagination-container {
      background: linear-gradient(135deg, var(--color-background) 0%, rgba(41, 42, 45, 0.8) 100%);
      border-color: var(--color-border);
    }

    .page-numbers {
      background: rgba(0, 0, 0, 0.2);
    }

    .page-number.active {
      box-shadow: 0 4px 12px rgba(66, 133, 244, 0.4);
    }

    .pagination-info {
      text-shadow: 0 1px 2px rgba(66, 133, 244, 0.2);
    }

    .section-title {
      color: var(--color-text);
    }
  }

  /* 🌗 强制深色主题适配 - 当body或html有dark-theme类时 */
  html.dark-theme .section-title,
  body.dark-theme .section-title,
  .dark-theme .section-title {
    color: var(--color-text) !important;
  }

  /* 🚀 高性能动画优化 */
  .pagination-btn,
  .page-number {
    will-change: transform, box-shadow;
  }

  .pagination-btn:not(:hover):not(:focus),
  .page-number:not(:hover):not(:focus) {
    will-change: auto;
  }
</style>
