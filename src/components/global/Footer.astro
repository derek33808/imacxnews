---
import { categories } from '../../data/categories';
---

<footer class="site-footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-logo">
        <a href="/" aria-label="Go to homepage">
          <span class="logo-text">IMACX</span><span class="logo-accent">News</span>
        </a>
        <p class="tagline">Delivering accurate news and insightful analysis</p>
      </div>
      
      <div class="footer-links">
        <div class="footer-section">
          <h3>Categories</h3>
          <ul>
            {categories.map(category => (
              <li>
                <a href={`/category/${category.id}`}>{category.name}</a>
              </li>
            ))}
          </ul>
        </div>
        
        <div class="footer-section">
          <h3>Company</h3>
          <ul>
            <li><a href="/about">About Us</a></li>
            <li><a href="/contact">Contact</a></li>
            <li><a href="/careers">Careers</a></li>
            <li><a href="/advertise">Advertise</a></li>
          </ul>
        </div>
        
        <div class="footer-section">
          <h3>Legal</h3>
          <ul>
            <li><a href="/terms">Terms of Use</a></li>
            <li><a href="/privacy">Privacy Policy</a></li>
            <li><a href="/cookies">Cookie Policy</a></li>
            <li><a href="/accessibility">Accessibility</a></li>
          </ul>
        </div>
        
        <div class="footer-section">
          <h3>Subscribe</h3>
          <p id="footerSubscriptionDescription">Get the latest news delivered to your inbox</p>
          <!-- 未订阅状态 -->
          <form class="newsletter-form" id="footerNewsletterForm">
            <input type="email" placeholder="Your email address" required>
            <button type="submit" class="btn">Subscribe</button>
          </form>
          <!-- 已订阅状态 -->
          <div class="subscribed-status" id="footerSubscribedStatus" style="display: none;">
            <div class="subscription-info">
              <span class="subscription-check">✓</span>
              <span class="subscription-message">Subscribed:</span>
              <span class="subscription-email" id="footerSubscriptionEmail">email@example.com</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <div class="footer-bottom">
      <p>&copy; {new Date().getFullYear()} IMACX-news. All rights reserved.</p>
      <div class="social-links">
        <a href="#" aria-label="Follow us on Twitter">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 4s-.7 2.1-2 3.4c1.6 10-9.4 17.3-18 11.6 2.2.1 4.4-.6 6-2C3 15.5.5 9.6 3 5c2.2 2.6 5.6 4.1 9 4-.9-4.2 4-6.6 7-3.8 1.1 0 3-1.2 3-1.2z"></path></svg>
        </a>
        <a href="#" aria-label="Follow us on Facebook">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path></svg>
        </a>
        <a href="#" aria-label="Follow us on Instagram">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
        </a>
        <a href="#" aria-label="Follow us on YouTube">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22.54 6.42a2.78 2.78 0 0 0-1.94-2C18.88 4 12 4 12 4s-6.88 0-8.6.46a2.78 2.78 0 0 0-1.94 2A29 29 0 0 0 1 11.75a29 29 0 0 0 .46 5.33A2.78 2.78 0 0 0 3.4 19c1.72.46 8.6.46 8.6.46s6.88 0 8.6-.46a2.78 2.78 0 0 0 1.94-2 29 29 0 0 0 .46-5.25 29 29 0 0 0-.46-5.33z"></path><path d="M9.75 15.02l5.75-3.27-5.75-3.27z"></path></svg>
        </a>
      </div>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background-color: var(--color-background-alt);
    color: var(--color-text);
    padding: var(--space-8) 0 var(--space-4);
    border-top: 1px solid var(--color-border);
    margin-top: 0; /* 由JavaScript自动计算并设置间距 */
    transition: 
      background-color var(--transition),
      border-color var(--transition),
      color var(--transition);
  }
  
  .footer-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-8);
    margin-bottom: var(--space-8);
  }
  
  .footer-logo a {
    display: inline-block;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text);
    text-decoration: none;
    margin-bottom: var(--space-2);
  }
  
  .logo-text {
    font-weight: 400;
  }
  
  .logo-accent {
    color: var(--color-primary);
  }
  
  .tagline {
    color: var(--color-text-light);
    margin-bottom: var(--space-4);
  }
  
  .footer-links {
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: var(--space-8);
  }
  
  .footer-section h3 {
    font-size: 1.125rem;
    margin-bottom: var(--space-4);
  }
  
  .footer-section ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .footer-section li {
    margin-bottom: var(--space-2);
  }
  
  .footer-section a {
    color: var(--color-text-light);
    text-decoration: none;
    transition: color var(--transition);
  }
  
  .footer-section a:hover {
    color: var(--color-primary);
  }
  
  .newsletter-form {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    margin-top: var(--space-2);
  }
  
  .newsletter-form input {
    padding: var(--space-2) var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background-color: var(--color-background);
    color: var(--color-text);
    width: 100%;
    transition: 
      border-color var(--transition),
      background-color var(--transition),
      color var(--transition);
  }
  
  .newsletter-form input:focus {
    outline: none;
    border-color: var(--color-primary);
  }
  
  /* 已订阅状态样式 - 简化版 */
  .subscribed-status {
    background-color: rgba(16, 185, 129, 0.05);
    border: 1px solid rgba(16, 185, 129, 0.2);
    border-radius: var(--radius-sm);
    padding: var(--space-3);
    margin-top: var(--space-2);
  }
  
  .subscription-info {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    flex-wrap: wrap;
  }
  
  .subscription-check {
    color: #10b981;
    font-size: 0.875rem;
    font-weight: bold;
  }
  
  .subscription-message {
    font-size: 0.75rem;
    color: var(--color-text-light);
    margin: 0;
  }
  
  .subscription-email {
    font-size: 0.75rem;
    color: var(--color-text);
    font-family: monospace;
    margin: 0;
  }
  
  
  .footer-bottom {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border);
    color: var(--color-text-light);
    font-size: 0.875rem;
    gap: var(--space-4);
  }
  
  .social-links {
    display: flex;
    gap: var(--space-4);
  }
  
  .social-links a {
    color: var(--color-text-light);
    transition: color var(--transition);
  }
  
  .social-links a:hover {
    color: var(--color-primary);
  }
  
  @media (min-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr 2fr;
    }
    
    .footer-links {
      grid-template-columns: repeat(4, 1fr);
      gap: var(--space-4);
    }
    
    .footer-bottom {
      flex-direction: row;
    }
  }
  
  @media (max-width: 767px) {
    .footer-links {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>

<script>
  // 🆕 Footer邮件订阅表单处理 - 简化版本
  const form = document.querySelector('#footerNewsletterForm');
  const subscribedStatus = document.getElementById('footerSubscribedStatus');
  const subscriptionEmail = document.getElementById('footerSubscriptionEmail');
  
  // 初始化
  document.addEventListener('DOMContentLoaded', () => {
    checkFooterSubscriptionStatus();
  });
  
  // 处理表单提交
  if (form) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const target = e.target as HTMLFormElement;
      if (!target) return;
      
      const submitBtn = target.querySelector('button[type="submit"]') as HTMLButtonElement;
      if (!submitBtn) return;
      
      // 禁用表单防止重复提交
      submitBtn.disabled = true;
      const originalText = submitBtn.textContent;
      submitBtn.textContent = 'Subscribing...';
      
      try {
        const response = await fetch('/api/newsletter/subscribe', {
          method: 'POST',
          credentials: 'include',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ source: 'footer' })
        });
        
        const result = await response.json();
        
        if (result.success) {
          alert('🎉 ' + result.message);
          if (form instanceof HTMLFormElement) {
            form.reset();
          }
          // 重新检查状态
          await checkFooterSubscriptionStatus();
          // 通知其他组件状态已更改
          window.dispatchEvent(new CustomEvent('newsletterStatusChanged', { 
            detail: { subscribed: true } 
          }));
        } else {
          if (result.error === 'LOGIN_REQUIRED') {
            if (confirm('📧 To subscribe to our newsletter, you need to have an account.\n\n' + result.message + '\n\nWould you like to login or register now?')) {
              if (window.openUserLoginModal) {
                window.openUserLoginModal();
              } else {
                window.location.href = '/#login';
              }
            }
          } else {
            alert('ℹ️ ' + result.message);
          }
        }
      } catch (error) {
        console.error('Newsletter subscription failed:', error);
        alert('❌ Failed to subscribe. Please try again later.');
      } finally {
        // 恢复表单状态
        submitBtn.disabled = false;
        submitBtn.textContent = originalText;
      }
    });
  }
  
  // 显示订阅表单
  function showFooterSubscriptionForm(email: string = '') {
    if (form instanceof HTMLElement) form.style.display = 'block';
    if (subscribedStatus instanceof HTMLElement) subscribedStatus.style.display = 'none';
    
    // 预填邮箱
    if (email && form) {
      const emailInput = form.querySelector('input[type="email"]') as HTMLInputElement;
      if (emailInput) {
        emailInput.value = email;
        emailInput.placeholder = 'Your email: ' + email;
      }
    }
  }
  
  // 显示已订阅状态
  function showFooterSubscribedStatus(email: string) {
    if (form instanceof HTMLElement) form.style.display = 'none';
    if (subscribedStatus instanceof HTMLElement) subscribedStatus.style.display = 'block';
    if (subscriptionEmail instanceof HTMLElement) subscriptionEmail.textContent = email;
  }
  
  // 检查Footer订阅状态
  async function checkFooterSubscriptionStatus() {
    try {
      const authResponse = await fetch('/api/auth/status', { credentials: 'include' });
      if (!authResponse.ok) {
        showFooterSubscriptionForm();
        return;
      }
      
      const authResult = await authResponse.json();
      if (!authResult.success || !authResult.user) {
        showFooterSubscriptionForm();
        return;
      }
      
      const subscriptionResponse = await fetch('/api/newsletter/subscribe', {
        method: 'GET',
        credentials: 'include'
      });
      
      if (subscriptionResponse.ok) {
        const result = await subscriptionResponse.json();
        if (result.subscribed) {
          showFooterSubscribedStatus(result.email || authResult.user.email);
        } else {
          showFooterSubscriptionForm(authResult.user.email);
        }
      } else {
        showFooterSubscriptionForm(authResult.user.email);
      }
    } catch (error) {
      console.error('Footer subscription status check failed:', error);
      showFooterSubscriptionForm();
    }
  }
  
  // 页面可见性变化时重新检查状态
  document.addEventListener('visibilitychange', () => {
    if (!document.hidden) {
      checkFooterSubscriptionStatus();
    }
  });
  
  // 监听其他组件的订阅状态变化
  window.addEventListener('newsletterStatusChanged', (e: any) => {
    console.log('📧 Footer: Received newsletter status change:', e.detail);
    checkFooterSubscriptionStatus();
  });
</script>
</script>